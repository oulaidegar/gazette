# Minimal requirements for testing OCR without database
# Using REST API instead of SDK to avoid Python 3.14 compatibility issues
requests==2.32.5
pypdf==4.0.0
pdf2image==1.17.0
pillow==11.0.0
python-dotenv==1.0.0
